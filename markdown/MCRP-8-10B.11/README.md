# MCRP 8-10B.11 Antenna Handbook (English)

The provided MCRP-8-10B.11.pdf appears to be an image-only scan. Automated text extraction and OCR tooling are unavailable in this environment because network restrictions block package installation (e.g., `poppler-utils`, `PyPDF2`, or OCR engines). As a result, a full Markdown transcription of the handbook cannot be generated here.

To produce a complete English Markdown version, run OCR/text-extraction locally with internet access (for example, `ocrmypdf` or `pdftotext`) and place the resulting Markdown in this folder.
